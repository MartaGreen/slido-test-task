import React from "react";
import { Stack } from "@mui/material";

function App() {
  return (
    <Stack
      sx={{
        width: "100%",
        height: "100vh",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      Hello, User!
    </Stack>
  );
}

export default App;
